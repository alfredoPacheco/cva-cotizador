export interface CustomerDto {
  id: string;
  name: string;
  // email: string;
  // phone: string;
  // address: string;
  // city: string;
  // state: string;
  // zip: string;
  // country: string;
  // notes: string;
}
